export enum Category {
  OVERHEAD_LOCKS = 'overhead-locks',
  MORTISE_LOCKS = 'mortise-locks',
  APARTMENT_LOCKS = 'apartment-locks',
  HOUSE_LOCKS = 'house-locks',
  HOTELS_LOCKS = 'hotels-locks',
  OFFICE_LOCKS = 'office-locks',
  LOCKERS_LOCKS = 'lockers-locks',
  CHANGING_ROOM_LOCKS = 'changing-room-locks',
}
